import 'package:flutter/material.dart';

  class AppTheme {
    static ThemeData get darkTheme {
      return ThemeData.dark().copyWith(
        // Aquí irán tus configuraciones de diseño y glassmorphism más adelante
      );
    }
  }